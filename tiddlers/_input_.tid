page-cover: https://source.unsplash.com/random
tags: 代码片段 表单
title: <input>
type: text/vnd.tiddlywiki

获取用户输入的组件，内置多种属性
联想：WPS表单里面的多种题目

!! 一、核心基础属性（必懂，控制输入类型与基础行为）

|! 属性名 |! 作用说明           |! 取值/示例|! 适用场景 |
| `type` | 定义输入框类型（决定输入形式） | `text`/`password`/`number`/`checkbox`/`radio`/`file`/`date` | 所有表单场景（核心属性） |
| `name` | 定义输入框名称（提交表单时的“键”，用于后端接收数据） | `username`/`email`| 表单提交、JS 获取数据|
| `value`| 设置输入框默认值（或选中状态的值） | `默认内容`| 预设默认输入、单选/复选框选中值 |
| `placeholder`| 输入框提示文本（未输入时显示，输入后消失） | `请输入用户名`| 引导用户输入（提升体验） |
| `id` | 输入框唯一标识（用于关联 `<label>`、JS 精准定位）| `user-email`| 绑定标签、JS 操作单个输入框|

!! 二、功能控制属性（控制输入规则与交互）

|! 属性名 |! 作用说明 |! 取值/示例|! 适用场景 |
| `required` | 标记为必填项（提交表单时未输入则提示报错） | `required`| 用户名、密码、邮箱等必填字段 |
| `disabled` | 禁用输入框（不可编辑、不可点击，值不提交） | `disabled`| 无需用户修改的固定信息 |
| `readonly` | 只读输入框（可查看、不可编辑，但值会提交） | `readonly`| 展示已确认的信息（如订单号） |
| `maxlength`| 限制输入的最大字符数（仅对文本类输入有效） | `20`| 用户名、密码长度限制 |
| `minlength`| 限制输入的最小字符数（部分浏览器支持） | `6` | 密码、验证码长度限制 |
| `pattern`| 用正则表达式验证输入格式（提交时校验） | `^[a-z0-9]+$` | 手机号、邮箱、验证码格式校验 |
| `min`/`max`| 限制数字/日期类型的输入范围| `18`/`2025-12-31` | 年龄、日期选择限制 |
| `step` | 数字/日期类型的步长| `5`/`7` | 数量选择、日期步进控制 |
| `multiple` | 允许选择多个值 | `multiple`| 多文件上传、多邮箱输入 |
| `autofocus`| 页面加载完成后自动获取焦点（默认选中该输入框） | `autofocus` | 登录页用户名输入框 |
| `autocomplete` | 控制自动补全（是否显示历史输入记录） | `on`/`off`| 隐私字段（如密码）关闭补全 |

!! 三、特殊场景属性（文件、单选/复选、隐藏输入）

|! 属性名 |! 作用说明 |! 取值/示例|! 适用场景 |
| `accept` | 限制文件上传的类型（仅对 `type="file"` 有效） | `image/*`/`.pdf,.doc` | 图片上传、指定格式文件上传 |
| `checked`| 单选框/复选框默认选中（仅对 `type="checkbox"`/`radio` 有效） | `checked`| 同意协议复选框、默认选项 |
| `hidden` | 隐藏输入框（页面不显示，但值会随表单提交） | `hidden`| 传递隐藏参数（如用户 ID）|

! 实际场景分析

1. 单选

给每个选项相同的name，他们就会单选。

理解：name创建了一个变量，单选是给name赋值，因为只能有一个值，所以变成了单选